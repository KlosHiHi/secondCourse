<Window x:Class="Task1.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task1"
        mc:Ignorable="d"
        Title="Авторизация" Height="450" Width="500">
    <Grid Background="DarkCyan">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>
        
        <Label x:Name="LoginLabel" Content="_Логин" Target="{Binding ElementName=LoginTextBox}"/>
        <TextBox x:Name="LoginTextBox" Grid.Column="1" MaxLength="30"/>

        <Label x:Name="PasswordLabel" Content="_Пароль" Grid.Row="1" Target="{Binding ElementName=PasswordBox}"/>
        <PasswordBox x:Name="PasswordBox" Grid.Column="1" Grid.Row="1" MaxLength="16"/>

        <Button x:Name="LoginButton" Grid.Row="2" Grid.ColumnSpan="2" Content="Войти" DockPanel.Dock="Top" Click="LoginButton_Click"/>

    </Grid>
</Window>
