<Window x:Class="LabWork32.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork32"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="TileElementDataTemplate">
            <DockPanel Width="180">
                <Image DockPanel.Dock="Top" Source="/Images/productIcon.png" Width="70" Height="70" HorizontalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Top" Text="{Binding Name}"/>
                <TextBlock DockPanel.Dock="Left" Text="{Binding Price, StringFormat=.0 Руб.}"/>
                <Button x:Name="BuyProductButton" Content="Купить" Height="30" Width="60" HorizontalAlignment="Right" Click="BuyProductButton_Click"/>
            </DockPanel>
        </DataTemplate>

        <DataTemplate x:Key="ListElementDataTemplate">
            <WrapPanel>
                <Image Source="/Images/productIcon.png" Width="70" Height="70"/>
                <TextBlock Text="{Binding Name}" Width="100"/>
                <DockPanel>
                    <Button x:Name="BuyProductButton" Content="Купить" Height="30" Width="60" Click="BuyProductButton_Click" DockPanel.Dock="Bottom"/>
                    <TextBlock Text="{Binding Price, StringFormat=.0 Руб.}"/>
                </DockPanel>
            </WrapPanel>
        </DataTemplate>

        <ItemsPanelTemplate x:Key="TileElementsItemsPanelTemplate">
            <WrapPanel/>
        </ItemsPanelTemplate>

        <ItemsPanelTemplate x:Key="ListElementsItemsPanelTemplate">
            <StackPanel/>
        </ItemsPanelTemplate>

    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="1" Grid.RowSpan="2" Grid.Column="1">
            <TextBlock DockPanel.Dock="Top" x:Name="SortByTextBlock" Text="Сортировка: сначала недорогие"/>
            <TextBlock DockPanel.Dock="Top" x:Name="FiltersTextBlock" Text="Фильтры: "/>
            <TextBlock DockPanel.Dock="Top" x:Name="CategoryTextBlock" Text="Категория: "/>
            <ToggleButton DockPanel.Dock="Top">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="TileElementButton" Content="Плитка" Click="TileElementButton_Click"/>
                    <Button x:Name="ListElementButton" Content="Список" Click="ListElementButton_Click"/>
                </StackPanel>
            </ToggleButton>

            <ListView x:Name="ProductsListView" ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                      SelectionMode="Extended" 
                      ItemsPanel="{StaticResource TileElementsItemsPanelTemplate}"
                      ItemTemplate="{StaticResource TileElementDataTemplate}">
            </ListView>
        </DockPanel>

        <ComboBox Grid.Row="1" x:Name="SortByComboBox" SelectionChanged="SortByComboBox_SelectionChanged">
            <ComboBoxItem IsSelected="True" Content="сначала недорогие"/>
            <ComboBoxItem Content="сначала дорогие"/>
            <ComboBoxItem Content="по новинкам"/>
            <ComboBoxItem Content="по скидке"/>
            <ComboBoxItem Content="по количеству отзывов"/>
            <ComboBoxItem Content="сначала с лучшей оценкой"/>
        </ComboBox>

        <DockPanel Grid.Row="2">
            <Button x:Name="ApplyFilterButton" Content="Применить" DockPanel.Dock="Bottom" Click="ApplyFilterButton_Click" Height="30"/>
            <ListBox x:Name="AvailabilityFilterListBox">
                <CheckBox IsChecked="True" Content="в наличии"/>
                <CheckBox IsChecked="True" Content="под заказ: сегодня"/>
                <CheckBox IsChecked="True" Content="под заказ: завтра"/>
                <CheckBox IsChecked="True" Content="под заказ: позже"/>
                <CheckBox Content="нет в продаже"/>
            </ListBox>
        </DockPanel>

        <ListBox Grid.ColumnSpan="2" x:Name="CategoryListBox" SelectionChanged="CategoryListBox_SelectionChanged">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <DockPanel Grid.Column="2" Grid.RowSpan="3">
            <Button x:Name="DeleteBasketProducButton" DockPanel.Dock="Bottom" Content="Удалить" Click="DeleteBasketProducButton_Click"/>
            <ListBox x:Name="ProductsBasketListBox" DisplayMemberPath="Name" SelectionMode="Extended"/>
        </DockPanel>
    </Grid>
</Window>
