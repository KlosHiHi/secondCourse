<Window x:Class="MediaPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer"
        mc:Ignorable="d"
        Title="Медиа плеер" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="MediumPurple"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="DarkGray"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <DockPanel Margin="10">
        <DockPanel DockPanel.Dock="Bottom">
            <StackPanel>
                <TextBlock Name="StatusTextBlock" Text="Не играет..." FontSize="20" Margin="5"/>
                <WrapPanel>
                    <Button Name="PlayButton" Click="PlayButton_Click" Content="▶"/>
                    <Button Name="PauseButton" Click="PauseButton_Click" Content="⏸"/>
                    <Button Name="StopButton" Click="StopButton_Click" Content="⏹"/>
                    <Button Name="SelectFileButton" Click="SelectFileButton_Click"  Content="🎬"/>
                </WrapPanel>
            </StackPanel>
            <ListView x:Name="VideosListView" SelectionChanged="VideosListView_SelectionChanged"/>
        </DockPanel>
        
        <MediaElement x:Name="MainMediaElement" Source="back.mp4" LoadedBehavior="Manual"/>

    </DockPanel>
</Window>
